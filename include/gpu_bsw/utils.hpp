#ifndef UTILS_HPP
#define UTILS_HPP

#include <omp.h>
#include <string>
#include <vector>

int get_new_min_length(const short *const alAend, const short *const alBend, const int blocksLaunched);
#endif